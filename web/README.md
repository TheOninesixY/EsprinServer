# EsprinNemo 网页版

由 EsprinServer 在根路径 `/` 直接托管的 EsprinNemo 网页客户端。
界面与桌面版 EsprinNemo 的**现代布局**逐条对应：同一套设计令牌与组件样式
（`tokens.css`、`base.css`、`sidebar.css`、`editor.css`、`overlays.css`、`settings.css`、
`ai.css`、`alom.css`、`radius.css`、`mode.css`、`motion.css` 与桌面版同名同源），
同一个默认布局（不排标题栏、标签页在工作区顶部、工具按钮悬浮在右上角），
同一套交互（自绘下拉、标签拖动排序与中键关闭、右键菜单、圆角滑块、Alom 风格、换肤交叉淡入）；
数据格式也用同一套内嵌元数据注释，因此网页版与桌面客户端可以被同一份同步日志互相认下。

## 目录

```
web/
├── index.html          页面骨架（标题栏 / 侧边栏 / 笔记列表 / 工作区 / AI 面板 / 小本本 / 设置）
├── favicon.png         站点图标
├── styles/             与桌面版同名同源，取值逐个对齐
│   ├── tokens.css      设计令牌：色板、圆角基础值、字体、动效
│   ├── base.css        重置、标题栏与标签页（含拖动排序）、通用按钮
│   ├── sidebar.css     侧边栏与笔记列表
│   ├── editor.css      工作区、编辑器与 Markdown 预览
│   ├── overlays.css    自绘下拉、右键菜单、Toast、消息弹窗
│   ├── settings.css    设置视图、开关、圆角滑块与字体预览
│   ├── ai.css          AI 助手面板与设置项
│   ├── scratchpad.css  小本本浮层（桌面版是独立窗口，网页版改为页内浮层）
│   ├── alom.css        Alom 风格（<html data-theme-style="alom">）
│   ├── radius.css      圆角尺度换算，需排在 alom.css 之后
│   ├── mode.css        现代 / 经典布局，与桌面版逐条对应
│   ├── motion.css      进场动画与换肤交叉淡入
│   └── web.css         网页版专属：同步状态入口、右侧走廊宽度、窄屏排版
└── scripts/
    ├── boot.js         首屏外观引导（主题 / 风格 / 圆角 / 应用名颜色 / 布局，渲染前落定）
    ├── markdown.js     轻量 Markdown 渲染器（含链接协议白名单）
    ├── store.js        条目模型、文件格式、IndexedDB / localStorage 持久化
    ├── ui.js           转义、时间格式、Toast、消息弹窗、自绘下拉
    ├── sync.js         /sync 协议客户端：SHA-256、待推送队列、拉取与推送、连接与鉴权
    ├── notes.js        条目操作：新建、自动保存、置顶、废纸篓、导入导出
    ├── render.js       渲染：计数、文件夹、标签、标签页（含拖动排序与两端渐隐）、列表、工作区
    ├── mode.js         布局切换（现代 / 经典）与「禁用标签页」
    ├── ai.js           AI 助手面板：对话、流式回答、附件、Agent 模式、设置项绑定
    ├── scratchpad.js   小本本浮层：自动保存、关联笔记、选择笔记
    ├── settings.js     设置视图（编辑器与文本 / 外观与主题色 / 字体 / AI 助手 / 数据与存储 / 关于）
    └── app.js          主题、主题风格、字体、侧边栏形态、快捷键与启动流程
```

## 界面与桌面版的一致程度

| 项 | 状态 |
| --- | --- |
| 现代布局（默认） | 一致：无标题栏，标签页在工作区顶部，应用名在侧边栏，工具按钮悬浮右上角，设置页与标签栏各带一个「返回」 |
| 经典布局 | 一致：标题栏 + 标签栏；切换入口在「设置 → 外观与主题色 → 界面布局」 |
| 外观 | 一致：明暗主题、主题风格（默认 / Alom）、主题色（预设 + 取色器 + 十六进制）、应用名颜色、圆角尺度（滑块四档）、侧边栏收起、换肤交叉淡入与其余动效 |
| 交互 | 一致：自绘下拉（不用原生弹出层）、右键菜单、标签拖动排序 / 中键关闭 / 滚轮横滚、键盘上下键与 Esc |
| 编辑器 | 一致：编辑 / 分屏 / 预览、排版工具栏、实时预览、字符与字数统计、废纸篓只读态 |
| AI 助手 | 面板与行为一致（对话记录、附带范围、附件、流式回答、复制 / 插入 / 存为新笔记、Agent 模式与撤销）；密钥存放与请求路径不同，见下 |
| 小本本 | 行为一致（自动保存、未关联时保存即新建并关联、选择笔记）；形态是页内浮层而非始终置顶的独立窗口 |
| 字体 | 可分别设置界面 / 文档、西文 / CJK，带实时预览；浏览器不允许枚举本机字体，因此改为自由填写而不是下拉选择 |

### 浏览器里做不到的部分

- **界面尺寸（缩放比例）**：桌面版改 Chromium 缩放比例，浏览器里请用浏览器自带的缩放（`Ctrl` + `+` / `-`）。
- **托盘与驻留后台、开机自启、应用自更新**：都依赖 Electron 主进程。
- **数据存放位置**：浏览器不允许自选存储目录，本地副本存在 IndexedDB（不可用时退回 localStorage）。
- **AI Key 的保管**：桌面版交给系统密钥链、请求由主进程代理；网页版只能由页面直连站点，密钥保存在本浏览器（localStorage），并且可能受站点的 CORS 策略限制。

## 访问方式

服务端启动后直接访问根路径：

```
http://<服务器地址>:8686/
```

- `/` 返回网页版界面；
- `/health` 返回服务端状态 JSON（旧根路径 JSON 响应的去处，含 `passwordSet` / `tokenCount`）；
- `/sync/*` 为同步接口，`/admin` 为管理后台。

## 存储与鉴权

网页版把**托管它的这台 EsprinServer 直接当存储**：本地只保留一份用于加速启动与断线缓存的副本
（IndexedDB，不可用时退回 localStorage），读写最终都落在服务端的操作日志上。

启动流程：

1. 先用本地副本渲染首屏；
2. 探测 `/health`，再拿 `/sync/state` 试一次鉴权访问；
3. 通过则自动进入服务端存储：首次使用做一次全量接入（重放服务器日志 + 上传服务器没见过的本地条目），
   之后按设置的节奏增量拉取，本地改动在编辑后立即推送；
4. 被拒则弹出连接层，必须输入管理密码或访问令牌才能进入，连接层不提供跳过入口。

鉴权有两种方式，同源页面默认走前者：

| 方式 | 说明 |
| --- | --- |
| 管理密码会话 | 用 `/admin` 的管理密码登录，服务端下发 12 小时的同源 Cookie，`/sync` 接口直接复用；服务端同时校验 `Origin` 与 `Host` 是否一致 |
| 访问令牌 | 在 `/admin` 创建，或使用启动参数 `--token` 的值；填写后以 `Authorization: Bearer` 方式访问，保存在浏览器本地存储中 |

服务端对 `/sync/*` 一律要求凭据：未携带登录会话或访问令牌的请求（包括同源请求）都返回 401。
服务端尚未设置管理密码、也没有创建过令牌时，先在 `/admin` 设置密码或创建令牌；
以 `--token` 启动时，则在连接层填写该参数的值。

## 数据与同步

- **存储单位**：`notes/{id}.md`、`todos/{id}.md`，与桌面版数据目录里的文件同名同格式
  （内嵌 `<!--EsprinData … -->` 元数据注释），因此两端的哈希一致，可以互推互认。
- **推送**：本地保存文件后生成 put / del 操作入队，同路径的旧操作被合并，编辑停止 800ms 后推送。
- **拉取**：按 seq 递增应用远端操作；本地有未推送的同路径改动时保留本地（与桌面版 keep-local 策略一致）。
- **首次接入**：重放服务器全部日志（以服务器为准），再上传服务器「从未见过」的本地文件
  （用 `/sync/state` 的现存与删除记录判断，避免把已删除的旧副本重新推上去）。
- **队列**：待推送操作存在浏览器 localStorage（`esprin.nemo.outbox`），断网期间的改动不会丢。

## 与桌面版的对应关系

| 能力 | 桌面版 | 网页版 |
| --- | --- | --- |
| 界面 | Electron 渲染层，同套样式 | 同一套样式文件与默认（现代）布局 |
| 存储 | 数据目录中的 `notes/`、`todos/` 文件 | 服务端操作日志（本地副本为缓存） |
| 同步 | `src/main/sync_server.js`（操作日志 + outbox） | `scripts/sync.js`，协议与合并策略一致 |
| 鉴权 | 系统密钥链里的访问令牌 | 同源管理会话或访问令牌 |
| Markdown | `styles/` + `scripts/markdown.js` | 同一套规则与样式取值 |
| 主题 | 系统 / 深色 / 浅色，主题风格、主题色、圆角、应用名颜色 | 一致 |
| AI 助手 | 主进程代理请求，密钥在系统密钥链 | 页面直连站点，密钥在本浏览器 |
| 小本本 | 独立置顶窗口 | 页内浮层，行为一致 |
| 字体 | 下拉选择系统字体 | 自由填写（浏览器不允许枚举本机字体） |
| 托盘、开机自启、自动更新、界面尺寸、数据位置 | Electron 主进程能力 | 不提供（见上文「浏览器里做不到的部分」） |

附：网页版的 AI 对话（`ai_chats/{id}.json`）与小本本内容（`scratchpad.json`）只落在本浏览器的 IndexedDB 里，
**不**进同步队列，因此不会把对话记进服务器的操作日志；笔记与待办的推拉不受影响。

## 离线部署（图标字体）

页面通过 Google Fonts 加载 Material Symbols Rounded 与 Mohave。无外网访问时图标会退化为文字，
如需完全离线，把 npm 包 `material-symbols` 的 `rounded.css` 与字体文件复制到 `web/fonts/material-symbols/`，
再把 `index.html` 里对应的一行 `<link>` 换成本地路径即可。
